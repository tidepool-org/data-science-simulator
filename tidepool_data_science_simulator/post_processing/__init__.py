"""Post-processing computation modules that ship with the simulator package.

Only modules consumed by packaged code (e.g. projects.risk.gui_runner) live
here. The repo's top-level ``post_processing/`` directory still holds the ad hoc
analysis/reporting scripts that are not part of the installed distribution.
"""
